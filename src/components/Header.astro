---
const navItems = [
  { name: "Home", href: "/" },
  { name: "About Us", href: "/about-us" },
  { name: "Contact", href: "/contact" },
];
---

<div class="bg-primary text-white text-sm py-2">
  <div class="max-w-7xl mx-auto px-4 flex justify-between items-center">
    <span>Serving Orlando, Miami & Fort Myers, FL</span>
    <a href="tel:9547275410" class="hover:underline font-semibold" aria-label="Call Pocket Door Services">(954) 727-5410</a>
  </div>
</div>

<header class="bg-white shadow-sm sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 flex items-center justify-between h-20">
    <a href="/" class="flex-shrink-0">
      <img src="/images/logo.png" alt="Pocket Door Services - Expert Installation & Repair in Orlando, Miami & Fort Myers" class="h-14 w-auto" />
    </a>
    <nav class="hidden lg:flex items-center gap-8" aria-label="Main navigation">
      {navItems.map((item) => (
        <a href={item.href} class="font-heading text-lg tracking-wide text-primary hover:text-accent py-2">{item.name.toUpperCase()}</a>
      ))}
    </nav>
    <a href="tel:9547275410" class="hidden lg:inline-flex bg-accent text-white font-heading text-lg tracking-wide px-6 py-3 rounded hover:bg-blue-700 transition-colors" aria-label="Call now for a free estimate">CALL NOW</a>
    <button id="mobile-menu-btn" class="lg:hidden p-2" aria-label="Open navigation menu">
      <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" /></svg>
    </button>
  </div>
  <div id="mobile-menu" class="hidden lg:hidden bg-white border-t border-gray-100">
    <div class="px-4 py-4 space-y-3">
      {navItems.map((item) => (
        <a href={item.href} class="block font-heading text-lg tracking-wide text-primary py-2">{item.name.toUpperCase()}</a>
      ))}
      <a href="tel:9547275410" class="block bg-accent text-white font-heading text-lg tracking-wide px-6 py-3 rounded text-center mt-4">CALL NOW</a>
    </div>
  </div>
</header>

<script>
  document.getElementById('mobile-menu-btn')?.addEventListener('click', () => {
    document.getElementById('mobile-menu')?.classList.toggle('hidden');
  });
</script>
